TradeLab Android v1 - Mobile GitHub Upload Package

This package is intended for GitHub's mobile web uploader, which may flatten folders.
Upload ALL files in this folder to the repository root. Do not create folders.
The GitHub Actions workflow build-apk.yml reconstructs the required Android folder structure during the build.
After committing, open Actions -> Build TradeLab APK -> Run workflow.
